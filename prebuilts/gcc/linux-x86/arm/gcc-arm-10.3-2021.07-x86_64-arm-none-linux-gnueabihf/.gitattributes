# This repository is a vendor toolchain payload. Keep every payload file in
# Git LFS; keep this policy file itself as a normal text Git object.
/** filter=lfs diff=lfs merge=lfs -text
/.gitattributes -filter -diff -merge text

